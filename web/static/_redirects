# SPA fallback — every path delegates to the client-side router.
# Wisp's _redirects matcher includes query strings in the rule LHS, so
# explicit per-route rules stop matching as soon as a ?param is added.
/*    /index.html   200
